
import 'package:flutter/material.dart';

class AttendanceRateWidget extends StatelessWidget {

  const AttendanceRateWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}